
//Creamos este archivo para realizar la importacion de todas las clases que utilizaremos en el proyecto, exportamos las variables que necesitamos y luego lo importamos en el index

import { Todo } from './todo.class.js';
import { TodoList } from './todo-list.class.js';

export{
    Todo,
    TodoList,
}